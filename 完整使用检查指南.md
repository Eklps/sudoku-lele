# 数独乐乐项目完整使用检查指南

## 阶段一：环境检查 ✅

### 1. 检查Java版本
```bash
java --version
```
**要求**: Java 21 或更高版本

### 2. 检查Maven版本
```bash
mvn --version
```
**要求**: Maven 3.6 或更高版本

### 3. 检查项目依赖
```bash
cd /f%3A/mySudoku/sudoku-lele
mvn dependency:resolve
```

---

## 阶段二：项目编译和启动 🚀

### 1. 清理并编译项目
```bash
mvn clean compile
```

### 2. 运行所有测试
```bash
mvn test
```
**预期结果**: 53个测试全部通过

### 3. 启动项目（二选一）
**方式A: 使用Maven**
```bash
mvn spring-boot:run
```

**方式B: 打包后运行**
```bash
mvn clean package -DskipTests
java -jar target/sudoku-lele-0.0.1-SNAPSHOT.jar
```

**成功标志**: 看到 "Started SudoleleApplication" 日志

---

## 阶段三：后端API功能检查 🔧

### 1. 基础连接测试
```bash
curl http://localhost:8080/api/games
```

### 2. 创建游戏测试
```bash
curl -X POST "http://localhost:8080/api/games/import-string" \
     -H "Content-Type: application/json" \
     -d '{"puzzleString": "530070000600195000098000060800060003400803001700020006060000280000419005000080079"}'
```

### 3. 移动测试
```bash
curl -X PUT "http://localhost:8080/api/games/1/cell" \
     -H "Content-Type: application/json" \
     -d '{"row": 0, "col": 1, "value": 4}'
```

### 4. 重置功能测试
```bash
curl -X POST "http://localhost:8080/api/games/1/reset"
```

### 5. 提示功能测试
```bash
curl http://localhost:8080/api/games/1/hint
```

---

## 阶段四：前端界面检查 🖥️

### 1. 访问主页面
打开浏览器访问: `http://localhost:8080`

### 2. 检查界面元素
- [ ] 数独网格显示正常
- [ ] 数字输入按钮可用
- [ ] 重置按钮存在
- [ ] 提示按钮存在
- [ ] 撤销/重做按钮存在

### 3. 功能交互测试
- [ ] 点击空白格子能够选中
- [ ] 输入数字能够正常显示
- [ ] 重置按钮能够清空输入
- [ ] 提示按钮能够显示建议

---

## 阶段五：完整游戏流程验证 🎮

### 1. 开始新游戏
1. 刷新浏览器页面
2. 确认看到新的数独谜题

### 2. 进行游戏操作
1. 选择一个空白单元格
2. 输入数字 (1-9)
3. 验证数字是否正确显示
4. 尝试输入错误数字，检查是否有错误提示

### 3. 使用高级功能
1. 点击"提示"按钮获取建议
2. 使用"撤销"功能回退操作
3. 使用"重做"功能恢复操作
4. 使用"重置"功能回到初始状态

### 4. 完成游戏验证
1. 尝试填满整个数独网格
2. 检查是否有胜利提示
3. 验证游戏状态是否正确更新

---

## 常见问题排查 🔍

### 问题1: 端口占用
```bash
# 检查8080端口占用
netstat -ano | findstr :8080
# 或使用其他端口启动
java -jar target/sudoku-lele-0.0.1-SNAPSHOT.jar --server.port=8081
```

### 问题2: 前端500错误
1. 检查浏览器开发者工具Console
2. 检查后端日志输出
3. 确认API端点正常工作

### 问题3: API调用失败
1. 确认Content-Type设置正确
2. 检查JSON格式是否正确
3. 确认服务器正在运行

### 问题4: 数据不同步
1. 清除浏览器缓存
2. 确认前端调用的API端点正确
3. 检查跨域设置

---

## 性能验证标准 📊

### 响应时间标准
- 游戏创建: < 200ms
- 移动操作: < 100ms
- 提示计算: < 500ms
- 重置操作: < 50ms

### 内存使用监控
```bash
# 监控Java进程内存
jps
jstat -gc [进程ID]
```

---

## 完成检查清单 ✅

- [ ] 环境配置正确
- [ ] 项目编译成功
- [ ] 所有测试通过
- [ ] 后端服务启动成功
- [ ] API端点全部可用
- [ ] 前端界面正常显示
- [ ] 前后端集成工作正常
- [ ] 游戏功能完整可用
- [ ] 性能表现符合预期
- [ ] 错误处理机制正常

---

## 部署验证（可选） 🚀

如果需要部署到生产环境：

### 1. 构建生产版本
```bash
mvn clean package -Pprod
```

### 2. 环境变量配置
```bash
export SPRING_PROFILES_ACTIVE=prod
export SERVER_PORT=8080
```

### 3. 健康检查端点
```bash
curl http://localhost:8080/actuator/health
```

---

**完成时间估计**: 30-45分钟
**技能要求**: 基础命令行操作、浏览器使用
**联系支持**: 如遇问题，请提供具体错误信息和日志输出 